const employee = {
    name : "sharanya",
    city : "Hyderabad",
    state : "Telangana",
    country : "India",
}
/* for (let key in employee){
    // console.log(key)
    //To get both keys and values
    console.log(key, employee[key])

} */
console.log(Object.keys(employee)) //keys in array format
console.log(Object.values(employee)) //values in array